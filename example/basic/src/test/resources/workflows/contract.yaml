name: contract

features:
  - classpath:features/user/get-user.feature
  - classpath:features/post/create-post.feature

tags:
  include: ["@contract"]
  exclude: ["@ignore"]

env: production
dataset: extended
parallel: 1
